<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:VRCVideoCacher.ViewModels"
             xmlns:mi="using:Material.Icons.Avalonia"
             xmlns:l="using:Jeek.Avalonia.Localization"
             xmlns:ctl="using:VRCVideoCacher.Controls"
             x:Class="VRCVideoCacher.Views.DashboardView"
             x:DataType="vm:DashboardViewModel">
<Grid RowDefinitions="Auto, *">
    <Border
        Grid.Row="0"
        Background="#1a73e8"
        IsVisible="{Binding HasMotd}"
        Padding="12,8">
        <Border.Styles>
            <Style Selector="HyperlinkButton">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>
            <Style Selector="HyperlinkButton:pointerover /template/ ContentPresenter">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#d6e4ff"/>
            </Style>
            <Style Selector="HyperlinkButton:pressed /template/ ContentPresenter">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#b9d0ff"/>
            </Style>
        </Border.Styles>
        <Grid ColumnDefinitions="*, Auto">
            <TextBlock
                Grid.Column="0"
                Foreground="White"
                ctl:MarkdownText.Text="{Binding Motd}"
                TextWrapping="Wrap"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontWeight="Medium" />
            <Button
                Grid.Column="1"
                Command="{Binding DismissMotdCommand}"
                Background="Transparent"
                BorderThickness="0"
                Padding="4"
                Margin="8,0,0,0"
                VerticalAlignment="Center"
                ToolTip.Tip="Dismiss">
                <mi:MaterialIcon Kind="Close" Width="16" Height="16" Foreground="White"/>
            </Button>
        </Grid>
    </Border>

    <ScrollViewer Grid.Row="1">
        
        <StackPanel Spacing="20" Margin="10">
            <!-- Header -->
            <Grid ColumnDefinitions="*, Auto">
                <TextBlock Text="{l:Localize Dashboard}" FontSize="24" FontWeight="Bold"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                    <Button Command="{Binding RefreshDataCommand}" ToolTip.Tip="{l:Localize Refresh}">
                        <mi:MaterialIcon Kind="Refresh" Width="20" Height="20"/>
                    </Button>
                    <Button Command="{Binding ToggleHostCommand}" ToolTip.Tip="{l:Localize ToggleHostTooltip}">
                        <mi:MaterialIcon Kind="ToggleSwitch" Width="20" Height="20" Foreground="{Binding HostState, Converter={x:Static vm:BoolToStatusColorConverter.Instance}}"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Stats Cards -->
            <WrapPanel Orientation="Horizontal">
                <!-- Server Status -->
                <Border Background="#2d2d2d" CornerRadius="8" Padding="20" Margin="0,0,15,15" MinWidth="200">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <mi:MaterialIcon Kind="Server" Width="24" Height="24" Foreground="#81C784"/>
                            <TextBlock Text="{l:Localize ServerStatus}" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>
                        <TextBlock Text="{Binding ServerRunning, Converter={x:Static vm:BoolToStatusConverter.Instance}}"
                                   FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="{Binding ServerUrl}" Foreground="#888888" FontSize="12" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Cache Stats -->
                <Border Background="#2d2d2d" CornerRadius="8" Padding="20" Margin="0,0,15,15" MinWidth="200">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <mi:MaterialIcon Kind="Database" Width="24" Height="24" Foreground="#64B5F6"/>
                            <TextBlock Text="{l:Localize Cache}" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CachedVideoCount, StringFormat='{}{0} videos'}"
                                   FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                        <StackPanel Orientation="Horizontal" Spacing="5" Margin="0,5,0,0">
                            <TextBlock Text="{Binding TotalCacheSize, Converter={x:Static vm:FileSizeConverter.Instance}}"
                                       Foreground="#888888" FontSize="12"/>
                            <TextBlock Text="/" Foreground="#888888" FontSize="12" IsVisible="{Binding MaxCacheSize, Converter={x:Static vm:GreaterThanZeroConverter.Instance}}"/>
                            <TextBlock Text="{Binding MaxCacheSize, StringFormat='{}{0} GB'}"
                                       Foreground="#888888" FontSize="12"
                                       IsVisible="{Binding MaxCacheSize, Converter={x:Static vm:GreaterThanZeroConverter.Instance}}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Download Queue -->
                <Border Background="#2d2d2d" CornerRadius="8" Padding="20" Margin="0,0,15,15" MinWidth="200">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <mi:MaterialIcon Kind="Download" Width="24" Height="24" Foreground="#FFB74D"/>
                            <TextBlock Text="{l:Localize Downloads}" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>
                        <TextBlock Text="{Binding DownloadQueueCount, StringFormat='{}{0} in queue'}"
                                   FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="{Binding CurrentDownloadText}" Foreground="#888888" FontSize="12" Margin="0,5,0,0"
                                   TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                    </StackPanel>
                </Border>

                <!-- Cookies Status -->
                <Border Background="#2d2d2d" CornerRadius="8" Padding="20" Margin="0,0,15,15" MinWidth="200">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <mi:MaterialIcon Kind="Cookie" Width="24" Height="24" Foreground="#CE93D8"/>
                            <TextBlock Text="{l:Localize YouTubeCookies}" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CookieStatus}"
                                   FontSize="20" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Tools Status Card -->
                <Border Background="#2d2d2d" CornerRadius="8" Padding="20" Margin="0,0,15,15" MinWidth="220">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,5">
                            <mi:MaterialIcon Kind="Tools" Width="24" Height="24" Foreground="#4FC3F7"/>
                            <TextBlock Text="{l:Localize Tools}" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>

                        <Grid ColumnDefinitions="*, Auto">
                            <TextBlock Text="yt-dlp" Foreground="#CCCCCC" FontSize="13"/>
                            <TextBlock Grid.Column="1" Text="{Binding YtdlpStatus}" FontWeight="Bold" FontSize="13" Foreground="#4FC3F7"/>
                        </Grid>

                        <Grid ColumnDefinitions="*, Auto">
                            <TextBlock Text="Deno" Foreground="#CCCCCC" FontSize="13"/>
                            <TextBlock Grid.Column="1" Text="{Binding DenoStatus}" FontWeight="Bold" FontSize="13" Foreground="#AED581"/>
                        </Grid>

                        <Grid ColumnDefinitions="*, Auto">
                            <TextBlock Text="FFmpeg" Foreground="#CCCCCC" FontSize="13"/>
                            <TextBlock Grid.Column="1" Text="{Binding FfmpegStatus}" FontWeight="Bold" FontSize="13" Foreground="#FF8A65"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Video Players Control Card -->
                <Border Background="#2d2d2d" CornerRadius="8" Padding="20" Margin="0,0,15,15" MinWidth="220">
                    <StackPanel Spacing="10">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <mi:MaterialIcon Kind="Video" Width="24" Height="24" Foreground="#81C784"/>
                            <TextBlock Text="{l:Localize VideoPlayers}" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>

                        <Button Command="{Binding ToggleVideoPlayersCommand}"
                                HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Margin="0,5,0,0">
                            <Panel>
                                <TextBlock Text="{l:Localize Disable}" IsVisible="{Binding VideoPlayersEnabled}" FontWeight="Bold"/>
                                <TextBlock Text="{l:Localize Enable}" IsVisible="{Binding !VideoPlayersEnabled}" FontWeight="Bold"/>
                            </Panel>
                        </Button>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- Quick Actions -->
            <Border Background="#2d2d2d" CornerRadius="8" Padding="20">
                <StackPanel Spacing="15">
                    <TextBlock Text="{l:Localize QuickActions}" FontWeight="SemiBold" FontSize="16"/>
                    <WrapPanel Orientation="Horizontal">
                        <Button Command="{Binding OpenCacheFolderCommand}" Margin="0,0,10,10">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <mi:MaterialIcon Kind="FolderOpen" Width="16" Height="16"/>
                                <TextBlock Text="{l:Localize OpenCacheFolder}"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding SetupCookieExtensionCommand}" Margin="0,0,10,10">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <mi:MaterialIcon Kind="Cookie" Width="16" Height="16"/>
                                <TextBlock Text="{l:Localize SetupYouTubeCookieExtension}"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding ClearCookiesCommand}" Margin="0,0,10,10" IsEnabled="{Binding CookiesFileExists}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <mi:MaterialIcon Kind="Delete" Width="16" Height="16"/>
                                <TextBlock Text="{l:Localize ClearCookies}"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Grid>
</UserControl>
